import { observer } from 'mobx-react-lite'
import React from 'react'

const ContactUs = () => {
    return (
        <div>
            Contactdetails later
        </div>
    )
}

export default observer(ContactUs);
